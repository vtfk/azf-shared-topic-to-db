{
  "name": "azf-shared-topic-to-db",
  "description": "Save all messages from a tobic to db",
  "version": "1.0.0",
  "license": "MIT",
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "main": "TopicToDbHandler/index.js",
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "test": "standard && npm audit && ava",
    "test-offline": "standard && ava",
    "coverage": "nyc ava",
    "coveralls": "nyc ava && nyc report --reporter=lcov && cat coverage/lcov.info | coveralls",
    "standard-fix": "standard --fix",
    "refresh": "rm -rf node_modules && rm package-lock.json && npm install"
  },
  "keywords": [
    "azure",
    "topic",
    "cosmosdb"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vtfk/azf-shared-topic-to-db.git"
  },
  "bugs": {
    "url": "https://github.com/vtfk/azf-shared-topic-to-db/issues"
  },
  "homepage": "https://github.com/vtfk/azf-shared-topic-to-db#readme",
  "devDependencies": {
    "ava": "1.4.1",
    "coveralls": "3.0.3",
    "husky": "2.2.0",
    "nyc": "14.0.0",
    "standard": "12.0.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test"
    }
  },
  "dependencies": {
    "mongojs": "2.6.0"
  }
}
